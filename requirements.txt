# Streamlit Community Cloud reads this file at the repo root.
#
# Trimmed from findociq/app/requirements.txt upstream:
#   matplotlib, pyyaml — verified unused by the app (import-blocker run)
#   openpyxl          — its only caller, _plain_xlsx_export, lived in the
#                       Ingest view, which this mirror drops
#
# Keep the pins in step with findociq/app/requirements.txt upstream — sync.sh
# deliberately does NOT copy this file (upstream's root file is a `-r` pointer
# that does not resolve here), so a pin added there must be added here by hand
# or this deploy silently resolves something else.
streamlit==1.60.0
# PINNED, not >=2.0.0 — this mirror is where that floor bit. Community Cloud
# installed pandas 3, whose `str` dtype uses pd.NA for missing values (truthy,
# and it stringifies), and 162 of 342 table titles rendered as "nan".
pandas==3.0.5
altair>=5.0.0
pypdfium2>=5.8.0   # lazy: original-PDF page render in the Database view
